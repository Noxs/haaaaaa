const constants = {
    types : { OPEN : "Open", CLOSE : "Close", STANDALONE : "Standalone" },
    categories : { FOR : "For", IF : "If", VARIABLE : "Variable" },
    identifiers : { GENERIC_OPEN : "{%", GENERIC_CLOSE: "%}", STANDALONE_OPEN : "{{", STANDALONE_CLOSE : "}}"}
}

module.exports = constants;
